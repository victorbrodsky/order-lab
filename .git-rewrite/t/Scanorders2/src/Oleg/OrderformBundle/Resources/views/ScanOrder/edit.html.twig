{#{% extends '::base.html.twig' %}#}

{% extends "OlegOrderformBundle::Default/base.html.twig" %} 

{% block content %} 
{% block body -%}
    <h1>Order Info Edit</h1>

    <form action="{{ path('scanorder_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form) }}
        <p>
            <button class="btn btn-small btn-warning btn_margin" type="submit">Edit</button>
        </p>
    </form>
    
{#    
    {% if is_granted('ROLE_ADMIN') %}    
        <form action="{{ path('orderinfo_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button onclick="return confirm('Are you sure?');" class="btn btn-small btn-danger" type="submit">Delete</button>
        </form>   
    {% endif %}  
 #}   
    
{% endblock %}
{% endblock %}
