{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Block creation</h1>

    <form action="{{ path('block_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('block') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
