{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Specimen list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Procedure Type</th>
                <th>Paper</th>
                <th>Mrn</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('specimen_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.proceduretype }}</td>
                <td>{{ entity.paper }}</td>
                <td>{{ entity.mrn }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('specimen_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('specimen_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('specimen_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
