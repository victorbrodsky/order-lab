{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Specimen</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Procedure Type</th>
                <td>{{ entity.proceduretype }}</td>
            </tr>
            <tr>
                <th>Paper</th>
                <td>{{ entity.paper }}</td>
            </tr>
            <tr>
                <th>Mrn</th>
                <td>{{ entity.mrn }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('specimen') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('specimen_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('specimen_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
