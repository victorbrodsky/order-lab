{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UserRequest list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cwid</th>
                <th>Request</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('userrequest_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.cwid }}</td>
                <td>{{ entity.request }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('userrequest_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('userrequest_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('userrequest_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
