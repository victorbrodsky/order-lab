import React from 'react';

const Loading = ({page, pages}) => {
    return (
            <div>
                <br/>
                Loading page {page} of {pages}...
            </div>
    );
};

export default Loading;


