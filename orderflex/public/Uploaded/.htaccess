# Block direct HTTP access to the Uploaded directory.
# Files are now served only through the authenticated employees_file_view route.

Order allow,deny
Deny from all

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
